#include <bits/stdc++.h>
using namespace std;

int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;
    cin >> n;

    vector<int> l;
    for (int i = 0; i < n; i++)
    {
        cin >> l[i];
    }
    vector<int> r;
    for (int i = 0; i < n; i++)
    {
        cin >> r[i];
    }

    sort(l.begin(), l.end());
    sort(r.begin(), r.end());

    for (int i = 0; i < n; i++)
    {
        if (a[i] > b[j])
        {
        }
    }
}