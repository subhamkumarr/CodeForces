#include <bits/stdc++.h>
using namespace std;

bool intersect(int v[], int x[])

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int n;
    cin>>n;
    int x[n], v[n];

    for(int i=0; i<n; i++)
    {
        cin>>x[i];
    }
    for(int i=0; i<n; i++)
    {
        cin>>v[i];
    }

    double low=0.0, high=1e9, ans = -1.0
    int iteration = 80;
    while(iteration--)
    {
        double mid = (low+high)/2;

        if()
    }
}